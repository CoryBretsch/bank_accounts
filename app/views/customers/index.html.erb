<h1> Customers </h1>

<% @customers.each do |customer| %>
  <div><%= link_to customer.name, customer_path(customer) %>, created at: <%= customer.created_at %></div>
<% end %>

<div>
  <%= link_to 'New Customer', '/customers/new' %>
</div>